let initialState = {
    isLogin: false,
    fullName: '',
    email: '',
    password: ''
}

const userReducer = (state = initialState, action) => {
    switch (action.type){
        // default.return
    }
}